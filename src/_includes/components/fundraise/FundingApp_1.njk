<form>
    <div class="container-funding-app container-touch">
        <div class="columns">
            <div class="column is-full is-6-desktop is-offset-3-desktop" style="padding:1rem;">
                <div v-if="errors.length" class="has-text-danger has-background-white-bis" style="padding: 1rem;margin: 1rem 0 1rem 0;">
                    <b>Wajib diisi: </b>
                    <span v-for="error in errors">{{ '{{error}}' }},</span>
                </div>
                <div>
                    {% include "components/fundraise/Form.njk" %}
                    <br>
                    <div class="fra-button has-text-right">
                        <button class="button is-primary has-text-weight-bold order-2-tablet-only" v-on:click.prevent="checkForm">
                            <span>Lanjut</span>
                            <span class="icon is-medium">
                                <i class="mdi mdi-24px mdi-chevron-right"></i>
                            </span>
                        </button>
                        <a href="{{ pagination.href.previous }}" class="button is-danger is-light has-text-weight-bold">Batal</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>